"""CLI namespace.

Python implementation lives under ``core.cli.python_cli``. Additional
language-specific CLI shells can be placed as siblings (for example a
future ``rustCli`` package) without touching the Python runtime.
"""

__all__: list[str] = []
